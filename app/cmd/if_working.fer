// Test if-else control flow - all should pass

let x: i32 = 10;
let y: i32 = 20;
let flag: bool = true;

// Simple if statement
if x > 5 {
    let result = x + 1;
}

// If-else statement
if flag {
    y = 25;
} else {
    y = 15;
}

// If-else-if chain
if x < 5 {
    y = 1;
} else if x < 15 {
    y = 2;
} else {
    y = 3;
}

// Boolean operations work
if flag && (x > 0) {
    y = 100;
}

fn function1(x: i32) {
    let y = x + 10;
}

fn function2(x: i32) {
    let y = x + 20;
}

fn function3(x: i32) {
    let y = x + 30;
}

fn main() {
    function1(5);
    function2(10); 
    function3(15);
}

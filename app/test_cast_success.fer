let x: i32 = 42;
let y: i64 = x as i64;  // Valid cast: i32 -> i64 (widening)

// Test with literals  
let a: i64 = 100 as i64;
let b: f32 = 42 as f32;

// Valid same-type cast (no-op)
let same: i32 = x as i32;

// Valid upward casts using explicit casting of literals
let small: i8 = 10 as i8;
let bigger: i32 = small as i32;

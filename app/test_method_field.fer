// Test accessing method as a field (not calling it)
type Person struct {
    name: str,
    age: i32
};

fn (self: Person) getName() -> str {
    return self.name;
}

fn main() {
    let p: Person = @Person{
        name: "Alice",
        age: 30
    };
    
    // Try to access the method as if it were a field (this should fail but with a different error)
    let method = p.getName;
}

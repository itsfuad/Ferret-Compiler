// Test method error message 
type Source struct {
    x: i32,
    y: i32  // Source has extra field
};

fn (self: Source) methodA() -> i32 {
    return self.x;
}

// Source is missing methodB

type Target struct {
    x: i32
};

fn (self: Target) methodA() -> i32 {
    return self.x;
}

fn (self: Target) methodB() -> i32 {
    return self.x + 1;
}

fn main() {
    let source: Source = @Source{x: 1, y: 2};
    // This should fail - Source is missing methodB required by Target
    let target: Target = source as Target;
    //target.MethodA();
}

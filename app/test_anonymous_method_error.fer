// Test that methods cannot be called on anonymous structs
fn main() {
    let point = @struct{
        x: 10,
        y: 20
    };
    
    // This should fail - anonymous structs cannot have methods
    let result = point.getX();
}
